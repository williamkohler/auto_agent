<% provide(:title, 'All users') %>

  <div>
    <h1>All users</h1>
  </div>

<%= render 'find_user' %>

  <div>
    <ul class="users">
      <%= render @users %>
    </ul>
    <div class="center">
      <%= will_paginate %>
    </div>
  </div>
