<% provide(:title, @artist.name) %>
<h1><%= @artist.name %></h1>
<h4 class="center">
  <%= cap_first_letter @shows.count.humanize %> upcoming
  <%= "engagement".pluralize(@shows.count) %>
</h4>

<div>
  <%= render partial: "shows/show",
             collection: @shows, as: :show %>
</div>
